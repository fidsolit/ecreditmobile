import { createTheme } from "@rneui/themed";

export const theme = createTheme({
  lightColors: {
    primary: "#007bff",
    background: "#f4f4f4",
  },
  darkColors: {
    primary: "#007bff",
    background: "#333",
  },
});
